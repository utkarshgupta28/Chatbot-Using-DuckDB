# Chatbot-Using-DuckDB
This project focuses on creating a lightweight, local chatbot that utilizes DuckDB as the query engine for structured data. The chatbot can answer questions based on a dataset (e.g., CSV or Parquet files) and leverages natural language processing (NLP) to convert user input into SQL queries.
